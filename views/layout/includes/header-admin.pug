header.bg-indigo-600.p-2
    .container.mx-auto.flex.justify-between.items-center
        a(href="/")
            h1.text-2xl.text-white.font-extrabold.text-center Bienes 
                span.font-normal Raices

        .flex.justify-between.items-center
            p(class="bg-white rounded-lg m-2 py-2 px-4")=nombre
            nav(href="" class="my-5 text-sm md:flex md:items-center md:gap-3 font-bold text-white hidden")
                a(href="") Mis Propiedades
                a(href="") Mi Perfil

                form(action="/auth/cerrar-sesion" method="POST")
                    input(type="hidden" name="_csrf" value=csrfToken)
                    input(type="submit" class="bg-indigo-800 py-1 px-10 rounded-lg cursor-pointer" value="Cerrar Sesion") 